[222. 完全二叉树的节点个数](https://leetcode.cn/problems/count-complete-tree-nodes/)

给你一棵 **完全二叉树** 的根节点 `root` ，求出该树的节点个数。

[完全二叉树](https://baike.baidu.com/item/完全二叉树/7773232?fr=aladdin) 的定义如下：在完全二叉树中，除了最底层节点可能没填满外，其余每层节点数都达到最大值，并且最下面一层的节点都集中在该层最左边的若干位置。若最底层为第 `h` 层（从第 0 层开始），则该层包含 `1~ 2h` 个节点。

 

**示例 1：**

![img](https://assets.leetcode.com/uploads/2021/01/14/complete.jpg)

```
输入：root = [1,2,3,4,5,6]
输出：6
```

**示例 2：**

```
输入：root = []
输出：0
```

**示例 3：**

```
输入：root = [1]
输出：1
```

 

**提示：**

- 树中节点的数目范围是`[0, 5 * 104]`
- `0 <= Node.val <= 5 * 104`
- 题目数据保证输入的树是 **完全二叉树**

 

**进阶：**遍历树来统计节点是一种时间复杂度为 `O(n)` 的简单解决方案。你可以设计一个更快的算法吗？



## 解法1：遍历

```c
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     struct TreeNode *left;
 *     struct TreeNode *right;
 * };
 */
int postOrder(struct TreeNode* root)
{
    if (root == NULL) return 0;

    // int l = postOrder(root->left);  // L
    // int r = postOrder(root->right); // R
    // int ret = 1 + l + r;            // N

    return 1 + postOrder(root->left) + postOrder(root->right);
}

int countNodes(struct TreeNode* root) {
    return postOrder(root);
}
```

## 进阶解法：利用完全二叉树特性

```c
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     struct TreeNode *left;
 *     struct TreeNode *right;
 * };
 */
int postOrder(struct TreeNode* root)
{ 
    if (root == NULL) return 0;

    // 利用满二叉树的特性
    struct TreeNode* left_node = root->left;
    struct TreeNode* right_node = root->right;
    int left_depth = 0;
    int right_depth = 0;

    while (left_node)
    {
        left_node = left_node->left; 
        ++left_depth;
    }

    while (right_node)
    {
        right_node = right_node->right; 
        ++right_depth;
    }

    if (left_depth == right_depth) 
        return (2 << left_depth) - 1;  // 2d == 010bin。left_depth是“左子树”高度

    
    
    // int l = postOrder(root->left);
    // int r = postOrder(root->right);
    // int ret = l + r + 1;
    // return ret;

    return 1 + postOrder(root->left) + postOrder(root->right);
}

int countNodes(struct TreeNode* root) {
    return postOrder(root);
}
```

