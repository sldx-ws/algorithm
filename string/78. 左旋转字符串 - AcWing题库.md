[78. 左旋转字符串](https://www.acwing.com/problem/content/description/74/)

字符串的左旋转操作是把字符串前面的若干个字符转移到字符串的尾部。

请定义一个函数实现字符串左旋转操作的功能。

比如输入字符串`"abcdefg"`和数字 2，该函数将返回左旋转 2 位得到的结果`"cdefgab"`。

**注意：**

- 数据保证 n 小于等于输入字符串的长度。

#### 数据范围

输入字符串长度 [0,1000]。

#### 样例

```
输入："abcdefg" , n=2

输出："cdefgab"
```



## 解法

```c
void my_swap(char* a, char* b)
{
    char tmp = *a;
    *a = *b;
    *b = tmp;
}

void reverse(char* str, int left, int right)
{
    while (left < right)
    {
        my_swap(&str[left], &str[right]);
        ++left;
        --right;
    }
}

char* leftRotateString(char* str, int n){
    int len = strlen(str);
    
    reverse(str, 0, len - 1);
    reverse(str, 0, len - 1 -n);
    reverse(str, len - n, len - 1);
    
    return str;
}
```



