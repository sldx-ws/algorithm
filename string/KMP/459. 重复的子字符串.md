[459. 重复的子字符串](https://leetcode.cn/problems/repeated-substring-pattern/)

给定一个非空的字符串 `s` ，检查是否可以通过由它的一个子串重复多次构成。

 

**示例 1:**

```
输入: s = "abab"
输出: true
解释: 可由子串 "ab" 重复两次构成。
```

**示例 2:**

```
输入: s = "aba"
输出: false
```

**示例 3:**

```
输入: s = "abcabcabcabc"
输出: true
解释: 可由子串 "abc" 重复四次构成。 (或子串 "abcabc" 重复两次构成。)
```

 

**提示：**

- `1 <= s.length <= 10^4`
- `s` 由小写英文字母组成



## 解法1：移动匹配

```c
bool repeatedSubstringPattern(char* s) {
    int len = strlen(s);
    char k[2 * len + 1];
    k[0] = '\0';

    strcat(k, s);
    strcat(k, s);

    // 掐头：k + 1
    // 替代去尾：看s出现的位置是不是len 
    return strstr(k + 1, s) - k != len;
}
```

### 补充

```c
// 找dest末尾的'\0',拼上后自动加'\0'
// 返回dest
char *strcat(char *dest, const char *src); 

// 找到：返回第一次出现的地址
// 没找到：返回 NULL
char *strstr(const char *str, const char *substr);
```



## 解法2：KMP

```c
void getNext(char *pat, int *next, int len) 
{
    int i = 1;   // 从 1 开始（标准next数组）
    int j = 0;   // 前缀长度
	
	// 默认 next[0] = 0
    next[0] = 0; 

    while (i < len) 
    {
        if (pat[i] == pat[j]) 
        {
            j++;
            next[i] = j;
            i++;
        }
        else 
        {
            if (j != 0)
            {
                j = next[j - 1];
            }
            else
            {
                next[i] = 0;
                i++;
            }
        }
    }
}

bool repeatedSubstringPattern(char* s) {
    int len = strlen(s);
    int next[len];
    getNext(s, next, len);

    if (next[len - 1] > 0 && len % (len - next[len - 1]) == 0)
    {
        return true;
    }

    return false;
}
```

```c
void getNext(char *pat, int *next, int len) 
{
    int i = 0;  // 指向后缀末尾
	int j = -1;	// 指向前缀末尾
	
	// 默认 next[0] = -1
    next[0] = -1;
	
    while (i < len) 
	{
        if (j == -1 || pat[i] == pat[j]) 
		{
            i++;
            j++;
            next[i] = j;  // j 指向前缀末尾
        } 
		else 
		{
            j = next[j];
        }
    }
}

bool repeatedSubstringPattern(char* s) {
    int len = strlen(s);
    int next[len + 1];  // 注意是 len+1
    getNext(s, next, len);

    if (next[len] > 0 && len % (len - next[len]) == 0)
    {
        return true;
    }

    return false;
}
```

